import { DM_Sans, Palanquin_Dark } from 'next/font/google';

export const palanquin_dark = Palanquin_Dark({
  subsets: ['latin'],
  weight: '700',
});

export const dm_sans = DM_Sans({
  subsets: ['latin'],
  weight: ['200','500'],
});
